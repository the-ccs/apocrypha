import { expect,it } from "vitest"; import { validateHypothesis } from "../../../packages/domain/hypotheses/src/index.js";
const t="2026-09-19T00:00:00.000Z", claim=(id:string)=>({id:id as never,subject:"x",predicate:"is",object:id,description:id,epistemicClass:"OBSERVED" as const,observationIds:[],assumptionIds:[],createdAt:t,lifecycle:"ACTIVE" as const});
it("allows alternatives and rejects mutually exclusive accepted claims",()=>{const cs=[claim("clm_a"),claim("clm_b")],h=(id:string,accepted:string[])=>({id:id as never,label:id,acceptedClaimIds:accepted as never[],rejectedClaimIds:[],unresolvedClaimIds:[],activeAssumptionIds:[],createdAt:t});expect(validateHypothesis(h("hyp_1",["clm_a"]),cs,[],[]).status).toBe("VALID");expect(validateHypothesis(h("hyp_bad",["clm_a","clm_b"]),cs,[],[{id:"rel" as never,fromClaimId:"clm_a" as never,toClaimId:"clm_b" as never,type:"CONTRADICTS",mutuallyExclusive:true}]).status).toBe("INVALID");});
