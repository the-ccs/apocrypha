import { expect,it } from "vitest"; import { mkdtempSync,writeFileSync,readFileSync } from "node:fs"; import { tmpdir } from "node:os"; import { join } from "node:path"; import { SourceArtifactIngestor } from "../../packages/domain/evidence/src/index.js";
it("stores immutable hash-addressed source bytes",()=>{const d=mkdtempSync(join(tmpdir(),"apo-")),input=join(d,"one.txt");writeFileSync(input,"one");const i=new SourceArtifactIngestor(join(d,"objects"));const a=i.ingest("prj" as never,input,"TEXT","text/plain");expect(readFileSync(a.storagePath,"utf8")).toBe("one");writeFileSync(input,"two");const b=i.ingest("prj" as never,input,"TEXT","text/plain");expect(b.sha256).not.toBe(a.sha256);expect(readFileSync(a.storagePath,"utf8")).toBe("one");});
